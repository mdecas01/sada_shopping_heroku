
  $(document).on('change', function() {
    $('div#second').html('<%= escape_javascript select(:category, :category2_id, Category.where(level: 1), {}, {:onchange => "alert_me_test()"}) %>');
    //alert('Changed question');
  });