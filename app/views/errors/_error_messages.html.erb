<% if object.errors.any? %>
<section id="error_messages">
  <header>
    <h2>
      Sorry, the <%= object.class.name.downcase %> could not be saved. 
    </h2>
    <h3>
       Correct the following <%= pluralize(object.errors.count, "error") %>:
    </h3>
  </header>
  <ul>
    <% object.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
</section>
<% end %>
